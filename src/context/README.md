# `/src/context/`

This folder defines low-overhead React contexts. For heavy and frequently updated state, Zustand stores should be used instead.

Standard context candidates:
- Themes (e.g. System Theme detection contexts).
- Localization configurations.
