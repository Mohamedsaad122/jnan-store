# `/src/layouts/`

This folder holds layout containers that wrap groups of page components to ensure consistency.

Standard layouts:
- `DefaultLayout`: Includes main responsive header, bottom mobile nav, main body, and footer.
- `AuthLayout`: Minimal page layout for forms (Login, Register).
- `AdminLayout`: Sidebar-focused administration layout.
