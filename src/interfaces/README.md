# `/src/interfaces/`

This folder defines core application system interfaces and contract signatures (such as services blueprints or API drivers definitions). This promotes strict decoupling between consumers and implementations.
