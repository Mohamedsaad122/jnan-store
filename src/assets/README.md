# `/src/assets/`

This folder hosts static assets used throughout the application, such as:
- SVGs and custom vector icons.
- Branding images, logos, and badges.
- Standard mock images for cards/banners.
