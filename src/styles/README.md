# `/src/styles/`

This folder defines global stylesheets and layout style frameworks:
- `index.css`: Global base styles, Tailwind imports, dynamic color variable tables, custom font families, scrollbar styles, and Arabic layout details.
