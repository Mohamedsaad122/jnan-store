# `/src/config/`

This folder defines core application system configurations:
- App environment validations (checking that React variables are properly defined at startup).
- Metadata descriptions and default setup properties.
