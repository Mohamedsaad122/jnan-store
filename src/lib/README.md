# `/src/lib/`

This folder hosts third-party wrapper wrappers and settings profiles.

Standard setups:
- `queryClient.ts`: TanStack Query Client options and configurations.
- `utils.ts`: Classname merge configurations for Shadcn (using `clsx` and `tailwind-merge`).
