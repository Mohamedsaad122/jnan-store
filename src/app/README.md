# `/src/app/`

This folder contains application-wide bootstrap modules, provider aggregators, and the entry root component of the application. 

Key contents:
- Global provider components (Context wrappers, QueryClientProvider, RouterProvider).
- `App.tsx`: The main React component that sets up layouts, routing, and global settings.
