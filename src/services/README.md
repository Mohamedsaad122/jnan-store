# `/src/services/`

This folder hosts core client networking infrastructure:
- Axios HTTP client singleton setup.
- Interceptors to process Bearer authorization headers and refresh tokens.
- Global endpoint queries and service interfaces (e.g. backend integration).
