# `/src/pages/`

This folder contains page entry point components. These map directly to router endpoints.

All page components should be loaded asynchronously (lazy-loaded) inside routing to support code-splitting and lower the initial javascript bundle size.
