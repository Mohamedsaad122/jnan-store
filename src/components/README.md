# `/src/components/`

This folder holds generic, stateless, and reusable UI components. These should follow the atomic design principles and remain independent of any business logic or global state.

Examples:
- Standard buttons, form input fields, checkboxes.
- Loading spinners, skeletons, modals, alerts.
- Data tables, badges, tabs.
