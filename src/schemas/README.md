# `/src/schemas/`

This folder defines runtime and form validation schemas (such as Zod specifications).

Standard schemas:
- `auth.ts`: Schema validating email inputs, passwords, login requirements.
- `checkout.ts`: Form validations for delivery location details, name matching, and payment card details.
